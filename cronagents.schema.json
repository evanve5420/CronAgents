{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cronagents.dev/cronagents.schema.json",
  "title": "CronAgents Global Configuration",
  "description": "Schema for the cronagents.json global scheduler configuration file.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path or URL to this schema file."
    },
    "autoFeedback": {
      "type": "boolean",
      "default": false,
      "description": "Auto-trigger feedback evaluator after each run."
    },
    "maxRunHistory": {
      "type": "integer",
      "minimum": 0,
      "default": 50,
      "description": "Max run directories to keep. 0 = unlimited."
    },
    "copilotPath": {
      "type": "string",
      "default": "copilot",
      "description": "Path to the Copilot CLI binary."
    },
    "retentionDays": {
      "type": "integer",
      "minimum": 0,
      "default": 14,
      "description": "Delete run directories older than this many days. 0 = never delete."
    },
    "startupDelay": {
      "type": "string",
      "default": "5m",
      "pattern": "^[0-9]+(m|h|s)?$|^0$",
      "description": "Delay before first scheduler tick (e.g. '5m', '1h', '30s', '0')."
    },
    "logLevel": {
      "type": "string",
      "enum": ["debug", "info", "warn", "error"],
      "default": "info",
      "description": "Minimum log level for scheduler output."
    },
    "quietHours": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "properties": {
            "start": {
              "type": "string",
              "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
              "description": "Start of quiet hours in HH:MM format."
            },
            "end": {
              "type": "string",
              "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
              "description": "End of quiet hours in HH:MM format."
            }
          },
          "required": ["start", "end"],
          "additionalProperties": false
        }
      ],
      "default": null,
      "description": "Time window during which no agents are run. Null to disable."
    },
    "notifications": {
      "type": "boolean",
      "default": true,
      "description": "Global toggle for Windows toast notifications. When false, all per-agent notifyOnFailure settings are ignored."
    },
    "personalRepo": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "default": "~/.cronagents",
          "description": "Path to the personal agents repository. Tilde (~) expands to $HOME."
        },
        "userName": {
          "oneOf": [
            { "type": "string" },
            { "type": "null" }
          ],
          "default": null,
          "description": "GitHub handle override. Null = detect from git config github.user, gh auth status, git config user.name, or USERNAME."
        },
        "autoCommitFeedback": {
          "type": "boolean",
          "default": true,
          "description": "Automatically commit feedback-evaluator changes in the personal repo."
        },
        "defaultWorkingDirectory": {
          "oneOf": [
            { "type": "string" },
            { "type": "null" }
          ],
          "default": null,
          "description": "Default project directory agents work in. Null = allow all directories."
        }
      },
      "additionalProperties": false,
      "description": "Personal repository settings for agent definitions and scheduling."
    }
  },
  "additionalProperties": false
}
